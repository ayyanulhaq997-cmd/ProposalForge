SaaS Architecture & Data Isolation Check (Master vs. Host)Test Scenario: Verify that the Host panel and the Admin panel see only the data they are permitted to see.StepUser RoleActionExpected Result (PASS)AHost (New Account)Register a new account and log in. Navigate to the Host Dashboard.You should see an empty property list and Host-specific statistics.BHost (New Account)Use the Create New Property feature to add "The Test Villa" with a price of $100.Property is saved successfully.CAdmin (Seeded Account)Log in to the Admin Dashboard. Navigate to Property Management."The Test Villa" property is visible in the global list, and you can Edit or Delete it.DAdmin (Seeded Account)Navigate to Host Management.The new Host account created in Step A is listed.2. ⚡ Real-Time & Booking Integration CheckTest Scenario: Verify WebSockets for chat and calendar functionality.StepUser RoleActionExpected Result (PASS)AHost (Window 1)Log in. Navigate to Chat/Messages.The chat panel loads successfully.BGuest (Window 2)View "The Test Villa." Click the "Contact Host" button.A chat window opens with the Host (Window 1).CGuest (Window 2)Start typing a message.The Host (Window 1) must see a "Guest is typing..." indicator.DHost (Window 1)Navigate to the property calendar and Block the dates Dec 20-25.Switch to the Guest's booking panel. The dates Dec 20-25 must show as unavailable immediately without refreshing.3. 💳 Payment & Security Check (Requires Your Stripe Test Keys)Test Scenario: Verify that the payment gateway integrates correctly and the system handles the booking status transition automatically.StepUser RoleActionExpected Result (PASS)AGuestSelect Dec 26-27 on "The Test Villa" (2 nights @ $100/night).The Booking Panel correctly calculates the total price ($200 + fees = Total).BGuestProceed to the payment page. Enter a Stripe Test Card number (e.g., 4242...4242).The payment is simulated and goes through successfully.CAdminCheck the Booking Management section.The new booking status must automatically update from "Pending" to "Confirmed" (This verifies the Stripe Webhook).DSecurityCheck the browser address bar.A Padlock icon is visible, confirming HTTPS is active.