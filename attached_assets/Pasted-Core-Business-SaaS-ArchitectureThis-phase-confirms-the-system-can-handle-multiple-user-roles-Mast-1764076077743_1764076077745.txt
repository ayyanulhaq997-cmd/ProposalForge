Core Business & SaaS ArchitectureThis phase confirms the system can handle multiple user roles (Master Admin, Host, Guest).RequirementTest Action (As the Client)Expected Result (PASS)SaaS Separation (Host/Admin)1. Register a new Host Account. 2. Login to the main Admin Panel (your account).You must see the newly created Host's profile in the Admin > Host Management section.Data IsolationAs the new Host, upload a property. Then, as Admin, check the Property CRUD section.The Admin must be able to see and manage the Host's property. The Host can only see their own properties.CustomizationIn the Admin Dashboard, find the CMS/Settings section. Change the main website's Hero Headline and Hero Background Image.The change must appear on the live public website immediately.Analytics/StatisticsCheck both the Admin Dashboard and the Host Dashboard.The Admin must see Global Revenue and Occupancy charts. The Host must see only Their Own Property revenue and booking data.2. ⚡ Real-Time Functionality & DesignThis phase verifies the use of technologies like WebSockets and the required UI/UX features.RequirementTest Action (As the Client)Expected Result (PASS)Real-Time Chat1. Log in as a Guest (Window 1). 2. Contact a Host (Window 2). 3. Send a message.The message appears instantly in Window 2. When the Host replies, the Guest sees a "Host is typing..." indicator.Availability & BookingLog in as a Host. Block out dates Dec 1-5 on your property calendar.The public Booking Widget for that property must instantly show Dec 1-5 as unavailable.Design: Dark ModeChange your computer/phone system's theme from Light to Dark Mode.The entire application UI (public site and dashboards) must switch colors automatically.ResponsivenessView the website on your mobile phone and a desktop computer.The content, search bar, and dashboards must adjust cleanly and be fully usable on both devices.Hero PageView the homepage.The Hero section must feature the background image you set via the Admin Panel and include smooth animations (not just static elements).3. 💳 Financial & Security ChecksThese are non-negotiable checks for a revenue-generating platform.RequirementTest Action (As the Client)Expected Result (PASS)Payment GatewayUse the payment page.It must show a secure checkout form powered by Stripe/Square, not a custom form.Booking & PricingTest booking for 1, 2, and 5 nights.The booking panel must correctly calculate the total price, including any service or cleaning fees.Booking ConfirmationComplete a Test Payment (using the developer's test key).The booking status in the Admin/Host panel must automatically update from 'Pending' to 'Confirmed' within seconds.Security & ID UploadAttempt to register a user.The system must prompt the user to upload a file (ID) for verification.Technical SEOOn the homepage, open the URL and check for the padlock icon.A Padlock icon must be visible, confirming a secure HTTPS connection.